{
  "name": "pricedeskly",
  "private": true,
  "version": "1.0.0",
  "description": "Study pricing model for clinical research services",
  "scripts": {
    "dev": "cd .. && wrangler pages dev",
    "deploy": "cd .. && wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
